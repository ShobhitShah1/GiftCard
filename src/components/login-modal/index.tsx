import React from 'react';
import { Modal, Text, View } from 'react-native';

export const LoginModal = () => {
  return (
    <Modal visible style={{ height: 50, backgroundColor: 'rgba(0,0,0,.6)' }}>
      <View className="bg-dark flex flex-row">
        <Text>SSS</Text>
      </View>
    </Modal>
  );
};
